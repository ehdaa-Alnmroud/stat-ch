import React from 'react'

const Part = () => {
  return (
    <div className="part">
      <h6>PRACTICE</h6>
      <h2>AT HOME OR ON-THE-GO ALWAYS ON YOUR TIME</h2>
      <p>
        Customize your membership and grow your practice by choosing from our
        expansive library of classes, courses, and articles.
      </p>
      {/* <button onClick={this.handelChange}>
          {" "}
          {this.state.visibility ? "hide" : "shwo"}
        </button>
        {this.state.visibility === true && <Containing />} */}
    </div>
  );
}

export default Part
